# Logger level: debug, info, warn, error
log_level = "info"

# Work directory to hold the state and history files.
# NOTE: Must end with a slash (/).
data_dir = "./data/jenkins/"

# The Jenkins to monitor.
[jenkins]
# Instance name (also names the state/history files, e.g., dragonfly.state)
name = "dragonfly"
# Base URL of the Jenkins
url = "https://ci.dragonflybsd.org/"
# Poll and state/history flush interval in seconds
interval = 60
# List of jobs to monitor
jobs = [
    "DragonFlyBSD",
    "DragonFlyBSD_RELEASE",
    "dragonflybsd-complete",
]

# Optional: the execution nodes to monitor (offline/online announcements).
# An empty list disables node monitoring entirely.
# nodes = ["Built-In Node"]

# A queued build (waiting for an executor) not started after this many
# seconds is announced as stuck; defaults to 300.
queue_stuck_after = 300

# Optional credentials for private instances (basic auth).
# user = ""
# password = ""
# api_token = ""

[webhook]
# Webhook URL
url = "http://127.0.0.1:2018/webhook"
# Request method
method = "POST"
# Bearer token to authenticate
token = "???"
# Name to identify this tool
from = "jenkins-monitor"
# Target to announce the messages
target = "#dragonflybsd"

[urlshort]
# Base URL of the URL shortener API, including the version prefix.
# Optional: when omitted, the full URLs are announced.
api = "https://example.com/.api/v1"
# Bearer token to authenticate with the URL shortener API
token = "???"
# Total HTTP request timeout in seconds
timeout = 5
